const FILES_TO_CACHE = [
    "/",
    "/db.js",
    "/index.html",
    "/index.js",
    "/style.css",
    "manifest.json",
];

const CACHE_NAME = "";
const DATA_CACHE_NAME = "",

self.addEventListener("Install", function(evt) {
    
})